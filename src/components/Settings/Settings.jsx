import React from 'react';
import styles from './Settings.module.css';

const Settings = () => {
    return (
        <div>
            Settings window
        </div>
    );
}

export default Settings;